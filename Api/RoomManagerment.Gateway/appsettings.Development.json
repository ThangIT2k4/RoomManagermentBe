{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ReverseProxy": {
    "Routes": {
      "identity": {
        "ClusterId": "identity",
        "Match": { "Path": "/identity/{**catch-all}" },
        "Transforms": [
          { "PathRemovePrefix": "/identity" }
        ]
      },
      "external": {
        "ClusterId": "external",
        "Match": { "Path": "/external/{**catch-all}" },
        "Transforms": [
          { "PathRemovePrefix": "/external" }
        ]
      },
      "notification": {
        "ClusterId": "notification",
        "Match": { "Path": "/notification/{**catch-all}" },
        "Transforms": [
          { "PathRemovePrefix": "/notification" }
        ]
      }
    },
    "Clusters": {
      "identity": {
        "Destinations": {
          "d1": { "Address": "${IDENTITY_API_URL}" }
        }
      },
      "external": {
        "Destinations": {
          "d1": { "Address": "${EXTERNAL_API_URL}" }
        }
      },
      "notification": {
        "Destinations": {
          "d1": { "Address": "${NOTIFICATION_API_URL}" }
        }
      }
    }
  }
}
